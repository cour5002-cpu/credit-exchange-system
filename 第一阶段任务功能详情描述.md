## 📋 第一阶段任务功能详情总结

### **项目概述**
- **项目名称**：课时兑换学分系统
- **技术栈**：Flask 3.0.3 + MySQL + Bootstrap 5.3.3
- **开发阶段**：第一阶段（最小可用版本）
- **核心功能**：课时申请审核流程 + 学分兑换流程

---

### **一、系统角色与权限**

#### **1. 学生（student）**
- 提交课时申请
- 查看申请列表和详情
- 查看课时账户
- 提交学分兑换申请
- 查看兑换申请列表和详情

#### **2. 教师（teacher）**
- 查看分配的待审核申请
- 审核课时申请（通过/驳回）
- 填写审核意见
- 修改最终认定课时
- 查看历史审核记录
- 下一个申请快速跳转

#### **3. 管理员（admin）**
- 查看全部课时申请
- 按状态筛选申请
- 分配审核教师（支持筛选）
- 查看申请详情
- 查看全部学分兑换申请
- 审核学分兑换申请

---

### **二、核心功能模块**

#### **模块1：课时申请流程**

**学生端功能：**
1. **提交课时申请** (`/student/hour-applications/new`)
   - 任务类别（下拉+输入）
   - 任务详情描述（200字限制，实时计数）
   - 参与人数（1-50人）
   - 参与人员（动态生成输入框）
   - 专业（下拉选择）
   - 课程（根据专业联动）
   - 课任/指导老师（根据专业+课程联动）
   - 申请课时数
   - 证明材料上传（多文件，最大10MB）
   - **前端验证**：必填字段为空显示红色小字"此字段不能为空"
   - **自动定位**：提交时自动滚动到第一个错误字段并聚焦
   - **异常处理**：网络/数据库错误显示弹窗提示

2. **我的课时申请列表** (`/student/hour-applications`)
   - 显示申请单号、任务类别、课程、状态、提交时间
   - 点击查看详情

3. **课时申请详情** (`/student/hour-applications/<id>`)
   - 显示完整申请信息
   - 查看证明材料
   - 查看审核记录
   - **已驳回申请不显示最终认定课时**

4. **我的课时账户** (`/student/hour-account`)
   - 累计获得课时
   - 已兑换课时
   - 剩余可用课时
   - 已兑换学分

**管理员端功能：**
1. **全部课时申请列表** (`/admin/hour-applications`)
   - 列顺序：操作、状态、审核教师、兑换课时、申请单号、课程、任务类别
   - 状态筛选：待分配、待审核、已通过、已驳回
   - 操作：分配教师/查看详情

2. **课时申请详情** (`/admin/hour-applications/<id>`)
   - 查看完整申请信息
   - 查看证明材料
   - 查看审核记录
   - 重新分配教师按钮
   - **已驳回申请不显示最终认定课时**

3. **分配审核教师** (`/admin/hour-applications/<id>/assign`)
   - 教师选择（可输入+下拉）
   - 按专业筛选
   - 按课程筛选
   - 按关键字筛选
   - **前端验证**：字段为空显示红色小字
   - **异常处理**：分配失败显示弹窗提示

**教师端功能：**
1. **申请待审核页** (`/teacher/hour-applications`)
   - 显示分配给当前教师的待审核申请
   - 列表显示：申请单号、任务类别、课程、申请课时、状态

2. **审核课时申请** (`/teacher/hour-applications/<id>`)
   - **"下一个"按钮位于右上角**
   - 查看申请详情
   - 查看证明材料
   - 审核操作：通过/驳回
   - 填写最终认定课时
   - 填写审核意见
   - 查看历史审核记录

3. **历史审核记录** (`/teacher/hour-applications/history`)
   - 查看已审核的申请

---

#### **模块2：学分兑换流程**

**学生端功能：**
1. **提交学分兑换申请** (`/credit-exchanges/new`)
   - 显示当前可用课时
   - 输入申请兑换课时数
   - 实时计算预计兑换学分（10:1比例）
   - 填写兑换说明
   - **前端验证**：课时不足提示
   - **异常处理**：提交失败显示弹窗

2. **我的兑换申请列表** (`/credit-exchanges`)
   - 显示兑换单号、申请课时、预计学分、状态、提交时间

3. **兑换申请详情** (`/credit-exchanges/<id>`)
   - 显示完整兑换信息
   - 查看审核意见

**管理员端功能：**
1. **全部兑换申请列表** (`/credit-exchanges/admin`)
   - 显示所有学生的兑换申请

2. **审核兑换申请** (`/credit-exchanges/admin/<id>`)
   - 查看兑换详情
   - 审核操作：通过/驳回
   - 填写审核意见
   - **异常处理**：审核失败显示弹窗

---

### **三、数据库设计**

#### **核心数据表**
1. `users` - 用户账号表
2. `students` - 学生信息表
3. `teachers` - 教师信息表
4. `task_types` - 任务类型字典表
5. `hour_applications` - 课时申请主表
6. `hour_application_attachments` - 申请附件表
7. `hour_application_reviews` - 审核记录表
8. `student_hour_accounts` - 学生课时账户表
9. `student_hour_transactions` - 课时流水表
10. `credit_exchange_applications` - 学分兑换申请表
11. `credit_exchange_records` - 兑换成功记录表
12. `system_configs` - 系统配置表

---

### **四、接口清单（路由）**

#### **公共接口**
- `GET/POST /auth/login` - 登录
- `GET /auth/logout` - 退出登录
- `GET /` - 首页

#### **学生端接口**
- `GET /student/dashboard` - 学生首页
- `GET/POST /student/hour-applications/new` - 提交课时申请
- `GET /student/hour-applications` - 我的课时申请列表
- `GET /student/hour-applications/<id>` - 课时申请详情
- `GET /student/hour-account` - 我的课时账户
- `GET/POST /credit-exchanges/new` - 提交学分兑换申请
- `GET /credit-exchanges` - 我的兑换申请列表
- `GET /credit-exchanges/<id>` - 兑换申请详情

#### **教师端接口**
- `GET /teacher/dashboard` - 教师首页
- `GET /teacher/hour-applications` - 申请待审核页
- `GET/POST /teacher/hour-applications/<id>` - 审核课时申请
- `GET /teacher/hour-applications/history` - 历史审核记录
- `GET /teacher/hour-applications/<id>/next` - 下一个申请

#### **管理员端接口**
- `GET /admin/dashboard` - 管理员首页
- `GET /admin/hour-applications` - 全部课时申请列表
- `GET /admin/hour-applications/<id>` - 课时申请详情
- `GET/POST /admin/hour-applications/<id>/assign` - 分配审核教师
- `GET /credit-exchanges/admin` - 全部兑换申请列表
- `GET/POST /credit-exchanges/admin/<id>` - 审核兑换申请

---

### **五、页面跳转流程**

#### **课时申请流程**
```
学生登录 → 学生首页 → 提交课时申请 → 我的课时申请列表 → 申请详情
                                                    ↓
管理员登录 → 管理员首页 → 全部课时申请列表 → 分配审核教师
                                                    ↓
教师登录 → 教师首页 → 申请待审核页 → 审核详情 → 提交审核
                                                    ↓
学生查看 → 申请详情 → 我的课时账户（查看课时变化）
```

#### **学分兑换流程**
```
学生登录 → 我的课时账户 → 提交学分兑换申请 → 我的兑换申请列表 → 兑换详情
                                                            ↓
管理员登录 → 管理员首页 → 全部兑换申请列表 → 审核兑换申请
                                                            ↓
学生查看 → 兑换详情 → 我的课时账户（查看课时和学分变化）
```

---

### **六、特色功能实现**

#### **1. 前端表单验证**
- 必填字段为空显示红色小字"此字段不能为空"
- 自动滚动并聚焦到第一个错误字段
- 输入框获得焦点显示蓝色边框
- 输入内容后错误提示自动消失
- 禁用浏览器默认验证弹窗

#### **2. 异常处理机制**
- 数据库错误捕获
- 网络错误捕获
- 文件上传错误捕获
- 统一显示红色弹窗提示
- 错误后保留表单数据，可重新提交

#### **3. 联动下拉选择**
- 专业 → 课程联动
- 专业 + 课程 → 教师联动
- 支持手动输入 + 下拉选择

#### **4. 空值统一显示**
- 所有页面空数据统一显示为"空"
- 不再使用"-"、"为空"、"无"等不同表示

#### **5. 用户体验优化**
- 任务详情描述字数实时计数
- 参与人员动态生成输入框
- 学分兑换实时计算预计学分
- 教师审核"下一个"按钮快速跳转
- 已驳回申请隐藏最终认定课时

---

### **七、测试账号**
- **管理员**：`admin` / `admin123`
- **教师**：`teacher1` / `teacher123` (共6个教师账号)
- **学生**：`student1` / `student123`

---

### **八、下一步优化建议**

#### **功能扩展**
1. **企业微信集成**
   - OAuth 登录
   - 消息通知（申请提交、审核完成等）
   - 组织架构同步

2. **高级筛选功能**
   - 管理员按专业/班级/时间范围筛选申请
   - 教师按任务类别筛选待审核申请
   - 学生按状态筛选自己的申请

3. **批量操作**
   - 管理员批量分配教师
   - 教师批量审核（相同结果）
   - 数据批量导出

4. **统计报表**
   - 学生课时统计
   - 教师审核工作量统计
   - 任务类别分布统计
   - 学分兑换趋势分析

5. **驳回后重新提交**
   - 允许学生修改被驳回的申请
   - 保留原申请记录
   - 审核记录关联

6. **多级审核**
   - 教师初审 → 管理员终审
   - 不同任务类别不同审核流程

7. **附件预览**
   - 在线预览图片、PDF
   - 附件下载功能

8. **操作日志完善**
   - 记录所有关键操作
   - 日志查询和导出

#### **性能优化**
1. 数据库索引优化
2. 分页加载（列表页）
3. 缓存常用配置
4. 文件上传进度显示

#### **安全加固**
1. CSRF 防护增强
2. 文件上传类型限制
3. SQL 注入防护
4. XSS 防护
5. 密码强度要求

#### **用户体验**
1. 移动端适配
2. 暗黑模式
3. 消息提醒（站内信）
4. 操作引导（新手教程）
5. 快捷键支持

---

### **九、技术债务**
1. 部分页面缺少加载状态提示
2. 错误日志未完整记录
3. 单元测试覆盖率不足
4. API 文档需要补充
5. 代码注释需要完善

---

**文档版本**：v1.0  
