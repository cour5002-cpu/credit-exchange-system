# V1 稳定基线记录

> 基线日期：2026-08-05  
> 基线用途：V2 架构治理开始前的后端行为对照与回退依据  
> 范围说明：本记录只确认本地后端、数据库迁移和自动化测试基线，不替代前端最终人工验收

## 1. 代码与运行环境

- 基线分支：`main`
- Python 运行环境：项目本地 `.venv`
- Flask 应用入口：`run.py`
- API 基础路径：`/api/v1`
- 数据库迁移工具：Flask-Migrate / Alembic

## 2. 数据库迁移基线

- 迁移代码最新版本：`c6e4f8a1d2b3`
- 本地数据库当前版本：`c6e4f8a1d2b3`
- 校验结果：`current == head`

校验命令：

```powershell
$env:FLASK_APP = "run.py"
.\.venv\Scripts\python.exe -m flask db heads
.\.venv\Scripts\python.exe -m flask db current
```

## 3. Flask 路由基线

- Flask 全部路由数量：158
- `/api/v1` 路由数量：132
- 路由集合 SHA-256：`f908876d4613dfaf5feb223077a729f1934af410653d6e1e60c43e077d9a5554`

哈希计算口径：

1. 读取 Flask `url_map`。
2. 每条记录使用 `路径|HTTP方法列表` 格式。
3. 排除自动生成的 `HEAD` 和 `OPTIONS`。
4. 对全部记录排序后使用换行连接。
5. 对 UTF-8 字节计算 SHA-256。

后续拆分 API 路由时，若没有书面接口变更，路由数量、路径、HTTP 方法和该哈希应保持不变。

## 4. 自动化测试基线

执行命令：

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests -v
```

执行结果：

```text
Ran 25 tests
OK
```

当前自动化测试覆盖的主要范围：

- API 时间与上海时区契约；
- 登录、退出、当前用户和未授权响应；
- 教师角色查询与管理员权限；
- 学生、教师和管理员导入；
- 任务类型管理；
- 有成果、无成果、草稿、补交和延期流程；
- 课时申请分配、审核及终审主线；
- 学分兑换主线及无生效规则场景；
- 投诉基础提交与管理员查看；
- 申诉及重新审核主线；
- 学院任务报名、筛选、队长和成果转课时流程；
- 附件类型、权限、软删除和导出角色边界。

## 5. 基线未证明的内容

本次阶段 0 没有自动证明以下事项：

- 前端所有页面已经完成最终人工验收；
- 浏览器中的页面展示、按钮状态和跳转全部正确；
- 真实多人并发审批不会产生竞争问题；
- 所有历史生产数据均可通过最新迁移；
- 性能、压力、安全扫描和生产部署已经完成；
- 25 项测试之外的所有异常分支均已覆盖。

因此，进入 V2 前仍需保留前端最终检查结论和必要的多角色人工冒烟记录。

## 6. 后续架构治理约束

在没有明确需求变更时，内部重构必须保持：

- API 基础路径不变；
- HTTP 方法与相对路径不变；
- Session Cookie 认证方式不变；
- 请求字段、响应字段及类型不变；
- `code/message/data` 响应包装不变；
- 业务错误码与 HTTP 状态码不变；
- 数据库字段、状态值和业务流转不变；
- 现有 25 项测试持续通过。

每一阶段重构完成后至少执行：

1. 应用创建和导入检查；
2. 路由基线对比；
3. 全量自动化测试；
4. 受影响角色和业务域的前端冒烟联调。

